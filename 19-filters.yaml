- name: scripting for filters
  hosts: local
  connection: local
  vars:
    topics: linux, shell, terraform, docker, ansible
    tags:
     name: ansible
     project: roboshop
     component: catalogue

  tasks:
  - name: greeting
    ansible.builtin.debug:
      msg: " Hi,kulasekhar {{ greeting | default('Good Morning')}}"
  - name: convert string to list
    ansible.builtin.debug:
      msg: "{{ topics | split(',') }}"
  - name: convert map to list
    ansible.builtin.debug:
      msg: "{{ tags | dic2items }}"